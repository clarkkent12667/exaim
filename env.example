# Environment Variables for ExAIm
# Copy this file to .env.local for local development
# For Vercel deployment, add these as environment variables in your Vercel dashboard

# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# OpenAI API Key (used in Supabase Edge Functions)
OPENAI_API_KEY=your_openai_api_key

# Note: VITE_ prefixed variables are exposed to the client-side
# Non-VITE variables are only available server-side (in Supabase Edge Functions)
